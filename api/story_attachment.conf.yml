accept_fields: [id, description, title, type, url, ext]
update_fields: [description, title, type, url, ext]
insert_fields: [description, title, type, url, ext]

type: node
fields:
    id:
        default: false
        desp: "The attachment's id."
        name: 'id'
        required: false
        type: string
    ext:
        desp: The extension field of attachment.
        name: ext
        type: dict
